# Smart Classroom Frontend Environment Variables

# API base URL (Render rewrite keeps /api on the same domain)
VITE_API_BASE_URL=/api

# WebSocket base URL (optional). Default resolves based on browser origin.
# Example for local dev: ws://localhost:8080/ws
VITE_WS_URL=

# Application metadata
VITE_APP_NAME=Smart Classroom
VITE_APP_VERSION=1.0.0

# Feature flags
VITE_AI_FEATURE_ENABLED=true
VITE_DEBUG_MODE=false
